import { render, screen } from "@testing-library/react";
import App from "../src/App";

test("Muestra mensaje de bienvenida", () => {
  render(<App />);
  expect(screen.getByText(/NeoCDT/i)).toBeInTheDocument();
});
